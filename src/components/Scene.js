import React from 'react';

const style = {
    backgroundColor: 'black',
    color: 'white',
    textAlign: 'center',
    fontWeight: 'bold',
    padding: '1% 0',
    width: '80%',
    marginLeft: 'auto',
    marginRight: 'auto'

};

const Scene = () => {
    return <div style={style}>EKRAN</div>
};

export default Scene;